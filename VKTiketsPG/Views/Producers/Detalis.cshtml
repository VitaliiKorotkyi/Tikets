@using static System.Collections.Specialized.BitVector32
@model Producer

@{
    ViewData["Title"] = "Producer Detalis";
}

<div class="container">
    <div class="row">
        <div class="col-md-8 offset-md-2">
            <p>
                <h1 class="text-center">Detalis for @Model.FullName</h1>
            </p>

            <hr />
            <div class="form-group text-center">
                <img class="border-info rounded-circle" style="max-width: 250px" id="ProfilePicturePreview" />
            </div>
            <div class="form-group">
                <label asp-for="ProfilePictureURL" class="control-label">Profile Picture URL</label>
                <input asp-for="ProfilePictureURL" class="form-control" readonly />
                <span asp-validation-for="ProfilePictureURL" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="FullName" class="control-label">Full Name</label>
                <input asp-for="FullName" class="form-control" readonly />
                <span asp-validation-for="FullName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Bio" class="control-label">Biography</label>
                <textarea asp-for="Bio" class="form-control" rows="4" readonly></textarea>
                <span asp-validation-for="Bio" class="text-danger"></span>
            </div>
            <div class="form-group text-center mt-4">
                <a class="btn btn-outline-success float-right" asp-action="Edit" asp-route-id="@Model.Id">Edit</a>

                <a asp-action="Index" class="btn btn-secondary ml-3">Show All</a>
            </div>
        </div>
    </div>
</div>


@section Scripts {
    <script>

        $(document).ready(function () {
            var output = document.getElementById('ProfilePicturePreview');
            output.src = $("#ProfilePictureURL").val();
        })
    </script>

}
